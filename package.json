{
  "name": "sudoku",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "concurrently": "^10.0.5"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "dev": "bun run --watch build.ts && bun x serve public",
    "build": "bun build src/main.ts --outdir public --minify && bun build worker.ts --outfile public/worker.js --minify",
    "buildfront": "concurrently \"bun build src/main.ts --outdir public --minify --watch\" \"tsc --noEmit --watch\"",
    "typecheck": "tsc --noEmit"
  }
}
